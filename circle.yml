dependencies:
  override:
    - pip install -U pip
    - make dev
    - pyenv local 2.7.11 3.5.2 2.6.9 pypy-5.3

test:
  pre:
    - make info
    - uname -a
    - lsb_release -a
  override:
    - make ci
